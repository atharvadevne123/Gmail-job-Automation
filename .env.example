# Path to your OAuth2 credentials JSON from Google Cloud Console
# Default: credentials.json in the same directory as the scripts
GMAIL_CREDENTIALS_PATH=/path/to/credentials.json

# Path where the token is saved after first login
# Default: token.pickle in the same directory as the scripts
GMAIL_TOKEN_PATH=/path/to/token.pickle
